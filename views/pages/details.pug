
extends ../shared/layouts/base

block content
    div.container-fluid
        h1 #{details.name}
        p #{details.desc}
        dl
            dt Price
            dd #{details.price} #{details.currency}
